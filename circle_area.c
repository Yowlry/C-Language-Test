#include <stdio.h>

// 给定两个圆形，使用函数判断哪个圆的面积更大
// Given two circles, use a function to determine which one has a larger area
double getCircle(double r)
{
	double radius;
	return radius;
}


int main()
{
	double c_r1 = getCircle(num1);
	double c_r2 = getCircle(num2);

	if (c_r1 > c_r2)
	{
		printf("%s\n", "the first circle is larger");
	}
	else if (c_r2 > c_r1)
	{
		printf("%s\n", "the second circle is larger");
	}
	else
	{
		printf("%s\n", "both circles are equal in size");
	}


	return 0;
}
